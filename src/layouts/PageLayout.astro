---
/**
 * PageLayout
 * Layout wrapper for standard content pages (non-landing)
 * Uses Navbar for consistent navigation across all pages
 * Differs from LandingLayout only in using simpler Footer (no CTA sections)
 */
import BaseLayout from './BaseLayout.astro';
import Navbar from '@/components/landing/Navbar.astro';
import Footer from '@/components/layout/Footer.astro';

interface Props {
  title?: string;
  description?: string;
  image?: string;
  imageAlt?: string;
  noindex?: boolean;
  nofollow?: boolean;
}

const { title, description, image, imageAlt, noindex, nofollow } = Astro.props;
---

<BaseLayout
  title={title}
  description={description}
  image={image}
  imageAlt={imageAlt}
  noindex={noindex}
  nofollow={nofollow}
>
  <Navbar slot="header" />

  <main class="min-h-screen pt-16">
    <slot />
  </main>

  <Footer slot="footer" />
</BaseLayout>
